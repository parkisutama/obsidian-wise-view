# Wise View - Copilot Custom Instructions

## Project Context & Identity

You are an expert TypeScript developer and Obsidian API specialist assisting in the development of **Wise View**, an Obsidian Community Plugin.
Wise View is a task-management-agnostic view enrichment plugin for Obsidian Bases. It provides richer data visualizations (Calendar, Swimlane, Gantt) without enforcing any specific task management workflow.

### Core Philosophy

- **Read-Only / Agnostic:** The plugin visualizes existing frontmatter data. It does NOT enforce schemas, manage task states, or create task hierarchies.
- **View Enrichment Only:** All logic related to task creation, quick capture, natural language date parsing, and recurrence algorithms has been intentionally stripped from the original codebase. Do not reintroduce them.

## Codebase Lineage & Architecture

This project is a heavily modified derivative work. When generating or modifying code, you must respect the existing architectural patterns derived from these sources:

1. **Base Architecture (Obsidian Planner):** - Origin: [SawyerRensel/Planner](https://github.com/anareaty/pretty-properties)
   - Retained: Calendar (FullCalendar), Kanban view implementations (the Kanban view is now named Swimlane).
   - Removed: Task list view, item modals, quick capture, `chrono-node` (date parsing), `rrule` (recurrence), and blocking dependencies.
2. **Gantt Integration:**
   - Library: [@jaeungkim/gantt-chart](https://github.com/jaeungkim/gantt-chart), a React library run on Preact through build aliases (`scripts/ui-runtime-aliases.mjs`). React itself is never installed.
   - Origin: the view was first built on Frappe Gantt, adapted from [lhassa8/obsidian-bases-gantt](https://github.com/lhassa8/obsidian-bases-gantt); that view was removed on 2026-09-20 (`docs/specs/gantt-frappe-removal.md`).
   - Objective: map Bases entries to the library's task model and write edits back to the user's own properties. The contract is `docs/specs/gantt-beta.md` (view id `wise-view-gantt`, displayed as "Gantt"; stored option keys are `gantt*`, never `ganttBeta*`).
3. **Styling & Color Configuration:**
   - Integration: [anareaty/pretty-properties](https://github.com/anareaty/pretty-properties)
   - Objective: Utilize existing color configurations and CSS variables provided by `pretty-properties` to theme the views (especially Swimlane cards and Gantt bars) rather than hardcoding new color logic.

## Technical Environment & Tooling

- **Language:** TypeScript (`"strict": true`).
- **Target:** Obsidian Desktop and Mobile (`isDesktopOnly: false`).
- **Bundler:** esbuild (Output: `main.js`).
- **Libraries:**
  - `obsidian` (Obsidian Plugin API)
  - `@jaeungkim/gantt-chart` and `preact` (Gantt chart visualization)
  - `fullcalendar` (Calendar visualization)

## AI Agent Directives

### 1. Feature Development (Do's)

- Focus strictly on mapping Obsidian frontmatter properties (e.g., `date_start`, `date_end`, `status`, `progress`) to the respective view engines (the Gantt chart library, FullCalendar).
- Ensure DOM elements created by third-party libraries (like the Gantt chart) are properly scoped within Obsidian's view containers (`this.containerEl`).
- Clean up all library instances, DOM listeners, and Obsidian events during view unmounting or plugin `onunload()`.
- Write clean, self-explanatory, and modular code. Avoid massive monolithic functions.

### 2. Strict Constraints (Don'ts)

- **NO Task Logic:** Do not write code to auto-update statuses or handle recurring tasks. Dependencies are the one exception, and only as specified in `docs/specs/gantt-beta.md` §3.5: derived blocking/conflict display and the optional schedule policies, all driven by the user's Depends on property.
- **NO Extraneous Modals:** Do not implement quick capture modals or task editing interfaces unless explicitly instructed to bridge a native Obsidian Base feature.
- **NO Hardcoded Styles:** Do not inject hardcoded HEX/RGB colors for task categories. Read and apply the CSS classes/variables generated by the `pretty-properties` integration.
- **NO Generic Solutions:** When implementing the Gantt view, account for Obsidian's specific theme switching (light/dark mode) and Base API data lifecycle.

### 3. Gantt-Specific Guidelines

- Map Bases entries to the library `Task` shape in `src/views/gantt-beta/taskMapping.ts`; the pure rules (dates, links, phases, sequence, cascade, derived status) live in `src/core/gantt/` and must not import `obsidian`.
- Dates are floating local time: `Date` properties are `YYYY-MM-DD` with an inclusive end, `Date & time` properties are `YYYY-MM-DDTHH:mm` with no offset. The library keeps them in UTC and reports edits as ISO strings ending in `Z`; that `Z` is not an instant. Always convert through `src/core/gantt/dates.ts`.
- Write only through the scoped grants in `src/platform/mutations/grants.ts` (`ViewRegistry.mutationsFor`). Never call `processFrontMatter` or import `LegacyMutationGateway` from `src/views/gantt-beta/` (the architecture guard fails it). See `docs/architecture/view-write-access.md`.
- Write links through `wikiLinkText` / the vault's link settings, never by wrapping text in `[[ ]]` yourself; damaged values must heal, not grow.
- Keep the whole-array `onTasksChange` diffing, the ordered write queue, and the echo gate intact when touching `writeBack.ts`.

### 4. UI & Copy Guidelines

- Use sentence case for UI elements.
- Ensure all views remain responsive to Obsidian's pane resizing.
- Delegate UI styling to `styles.css` using Obsidian's standard CSS variables (`var(--background-primary)`, `var(--text-normal)`, etc.) combined with `pretty-properties` variables.
